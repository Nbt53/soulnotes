<% layout('./layouts/boilerplate') -%>
    <section class="product">
        <% product=product[0] %>



            <div class="card">
                <div class="card-header">
                    <div href="/products/<%= product.name %>" class="card-title product__title u-mb-lrg">
                        <%= product.name %>
                    </div>
                    <img src="<%= product.image %>" alt="" class="card-img">

                </div>
                <div class="card-body">
                    <div class="card-item">£<%= product.price %>
                    </div>
                    <div class="card-item">
                        <%= product.qty %> left in stock
                    </div>
                    <div class="card-item"><%= product.description %></div>
                    <div class="card-item"><%= product.notesOne %></div>
                    <div class="card-item"><%= product.notesTwo %></div>
                    <div class="card-item"><%= product.notesThree %></div>
                </div>

                <div class="card-btn">
                    <% if(currentUser && currentUser.username==='soulnotesboss@gmail.com' ){ %>
                        <form action="/products/<%= product.name %>?_method=DELETE" class="card-delete" method="POST">
                            <button class="btn card__btn-btn btn-card">
                                Delete
                            </button>
                        </form>

                        <button class="btn card__btn-btn btn-card">
                            <a href="/products/edit/<%= product.name %>" class="link">Edit</a>
                        </button>
                        <% } %>


                            <button class="btn card__btn-btn btn-card">
                                <a href="https://buy.stripe.com/test_bIYeXs86eeHY2L69AA" class="link">Buy Now</a>
                            </button>

                </div>
            </div>



    </section>
    <%- include('../../partials/shipping') %>